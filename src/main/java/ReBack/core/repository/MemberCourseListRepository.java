package ReBack.core.repository;

import ReBack.core.data.MemberCourseList;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MemberCourseListRepository extends JpaRepository<MemberCourseList, String> {
}
